pipeline {
    agent any
    tools {
      maven 'mymaven'
      }
    stages {
        stage('build') {
            steps {
                sh 'mvn clean package'
            }
        }
       stage('deploy') {
            steps {
                sshagent(['betteronjenkins']) {
    // scp <src_dir> user@tomcatIP:/tomcat/webapps
    sh "scp -o StrictHostKeyChecking=no target/*.war good@3.133.108.160:/home/good/tomcat/webapps"
}
       }
           }

    }
}
